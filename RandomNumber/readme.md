# RandomNumber

An empty Android project template intended for building a random number generator. No source code has been added yet.

## Key Concepts

Once developed, this project is likely to cover:

- **Random number generation**: Using `kotlin.random.Random` or `java.util.Random` to generate numbers
- **UI interactions**: Wiring a button to trigger logic and display results
- **TextView updates**: Showing generated output in the UI

## Status

No Kotlin or Java source files have been created. The `AndroidManifest.xml` exists but declares no activities.
